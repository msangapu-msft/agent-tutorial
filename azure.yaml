name: app-service-agent-tutorial
metadata:
  template: local
services:
  web:
    project: ./src
    host: appservice
    language:custom
hooks:
  postprovision:
    shell: ./azure/hooks/postprovision.sh
